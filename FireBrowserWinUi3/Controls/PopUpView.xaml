<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FireBrowserWinUi3.Controls.PopUpView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FireBrowserWinUi3.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="White" Width="500" Height="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="Header" Orientation="Horizontal" Background="Gray" Padding="10" PointerPressed="Header_PointerPressed"  PointerReleased="Header_PointerReleased">
            <Button  HorizontalAlignment="Right" Click="CloseButton_Click" Margin="0,0,5,0">
                <FontIcon Glyph="&#xE8BB;" />
            </Button>
            <Button IsEnabled="False"  HorizontalAlignment="Right" Click="PopOut_Click" Margin="0,0,5,0">
                <FontIcon Glyph="&#xF7ED;" />
            </Button>
        </StackPanel>

        <WebView2 x:Name="webView"
                  Grid.Row="1"
                  Margin="10"/>
    </Grid>
</UserControl>

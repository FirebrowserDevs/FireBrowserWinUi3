<UserControl
    x:Class="FireBrowserWinUi3.Controls.PopUpView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Width="500" Height="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="Header" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}" 
              PointerPressed="Header_PointerPressed" PointerReleased="Header_PointerReleased">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Padding="8">
                <Button Style="{StaticResource ButtonRevealStyle}" Click="PopOut_Click" IsEnabled="False" Margin="0,0,4,0">
                    <FontIcon Glyph="&#xF7ED;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                </Button>
                <Button Style="{StaticResource ButtonRevealStyle}" Click="CloseButton_Click">
                    <FontIcon Glyph="&#xE8BB;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                </Button>
            </StackPanel>
        </Grid>

        <WebView2 x:Name="webView" Grid.Row="1" Margin="8"/>
    </Grid>
</UserControl>